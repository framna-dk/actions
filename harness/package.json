{
  "name": "banzai-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "ncc build src/preflight.ts -o dist-preflight && mv dist-preflight/index.js dist/preflight.js && rm -rf dist-preflight && ncc build src/harness.ts -o dist-harness && mv dist-harness/index.js dist/harness.js && rm -rf dist-harness",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "liquidjs": "^10.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.6.0"
  }
}
